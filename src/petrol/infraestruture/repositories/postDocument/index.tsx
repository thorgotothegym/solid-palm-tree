import api from "../../../../common/infraestructure/api";

/* export default async function postWithdrawFunds({
  bankAccountNumber,
}: {
  bankAccountNumber: string;
}) {
  const withdrawRequest = await api.post(
    `/documents`,
    adaptPostWithdrawFunds({ bankAccountNumber }),
  );
  return withdrawRequest;
}
 */