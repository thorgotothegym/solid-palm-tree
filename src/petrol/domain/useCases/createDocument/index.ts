/* import Employee from "../../entities/Employee";
import postInviteEmployees from "../../../infrastructure/repositories/postInviteEmployees";

export default function createDocument({
  employees,
}: {
  employees: Employee[];
}) {
  return postInviteEmployees({ employees });
}
 */
